// ContentView.swift

import SwiftUI

struct ContentView: View {
    var body: some View {
        // Show the WelcomeView first when the app launches
        WelcomeView()
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}

